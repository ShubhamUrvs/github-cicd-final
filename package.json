{
  "name": "github-actions-demo",
  "version": "1.0.0",
  "description": "A demo application for GitHub Actions CI/CD",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "echo \"No linting configured\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
